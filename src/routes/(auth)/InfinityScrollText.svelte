<script lang="ts">
  // import { createQuery } from "@tanstack/svelte-query";

  // import Marquee from "$lib/components/ui/marquee/marquee.svelte";

  // const goldResult = createQuery({
  //   queryKey: ["gold"],
  //   queryFn: async () => (await fetch(`/api/gold`)).json(),
  // });
</script>

<div class="w-full">
  <!-- {#if $goldResult.data?.buyingPrice}
    <Marquee speed={50}>USD: {$goldResult.data?.buyingPrice ?? 0}</Marquee>
  {/if} -->
</div>
